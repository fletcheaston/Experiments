# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
# Commit message hook
-   repo: local
    hooks:
    -   id: validate-commit-msg
        name: validate-commit-msg
        language: pygrep
        entry: ^(🐞|🎉|📝|🧪|🧹|💾)( ).*
        stages: [ commit-msg ]
        args: [ --negate ]

# Advent of Code hooks
-   repo: local
    hooks:
    -   id: advent-of-code/lint
        name: advent-of-code/lint
        entry: bash -c 'cd advent-of-code && make lint'
        language: system
        files: ^advent-of-code/
        types_or: [python]

-   repo: local
    hooks:
    -   id: advent-of-code/format
        name: advent-of-code/format
        entry: bash -c 'cd advent-of-code && make format'
        language: system
        files: ^advent-of-code/
        types_or: [python]

# Blog hooks
-   repo: local
    hooks:
    -   id: blog/lint
        name: blog/lint
        entry: bash -c 'cd blog && make lint'
        language: system
        files: ^blog/
        types_or: [python]

-   repo: local
    hooks:
    -   id: blog/format
        name: blog/format
        entry: bash -c 'cd blog && make format'
        language: system
        files: ^blog/
        types_or: [python]

# Blog component hooks
-   repo: local
    hooks:
    -   id: blog-components/lint
        name: blog-components/lint
        entry: bash -c 'cd blog-components && make lint'
        language: system
        files: ^blog-components/
        types_or: [ts, tsx]

-   repo: local
    hooks:
    -   id: blog-components/format
        name: blog-components/format
        entry: bash -c 'cd blog-components && make format'
        language: system
        files: ^blog-components/
        types_or: [ts, tsx]

# MyFreightCube hooks
-   repo: local
    hooks:
    -   id: my-freight-cube/lint
        name: my-freight-cube/lint
        entry: bash -c 'cd my-freight-cube && make lint'
        language: system
        files: ^my-freight-cube/
        types_or: [ts, tsx]

-   repo: local
    hooks:
    -   id: my-freight-cube/format
        name: my-freight-cube/format
        entry: bash -c 'cd my-freight-cube && make format'
        language: system
        files: ^my-freight-cube/
        types_or: [ts, tsx]
